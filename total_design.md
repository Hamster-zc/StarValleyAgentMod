# 总体架构
这是一个星露谷mod，旨在将内一个NPC变为独立agent，使其更加人性化
每个电脑单独运行小模型，每个NPC一个LLM实例或多NPC共享模型
对于显卡能力不足的电脑，发送请求到主机，主机通过维护agent节点，将请求分发到有空闲算力的agent届节点。

# 模块
## 前端
钩子来截取和npc的交流
初步仅限日常交流，节日等特殊交流后期再更新

## 网络/通讯层（弱机与主机/Agent节点间）
弱机/强机（主机/agent node）通信协议设计：
    请求结构：NPC名字/Agent编号、上下文、游戏日、玩家输入、环境信息
    响应结构：自然语言回复、行动计划（如move/give礼物/切换场景）、需要展示的对话选项等
    版本协商（协议update兼容）
    心跳/超时/断线重连机制（长连接建议WebSocket或gRPC）

安全性
    认证（如简单签名token），防止接口被滥用或有恶意mod绕用

## 后端
短期记忆（Working memory）
    单天内的即时对话、玩法事件、计划（例如玩家当天给了NPC什么东西）
    查询极快，游戏日终自动归档或丢弃

中期记忆
    一个月游戏内所有Agent短期记忆的聚合，做摘要/结构化
    可用wiki形式自然语言存储（如“莉亚本月与玩家关系加深，节日吃了牛排...”）
    存索引的同时要便于RAG检索

长期记忆
    重大历史事项/跨年重要事件/主线剧情等
    可做embedding/量化/摘要，低频检索（效率比容量重要），可压缩
    例：去年“春季舞会”与谁共舞、“大丰收节”获奖情况等

RAG组件（检索与生成）
    记忆/游戏数据定期“embedding向量化”存入本地（主机）向量数据库（Faiss、Milvus、DuckDB等均可）
    Agent推理时按“上下文+模糊检索”pull相关记忆

存储机制
    必须主机本地持久化（确保单点一致、同步/归档安全）
    可选zip、日记/日志、SQLite、简易文本+向量，仅主机存档参与同步

## 主控/Agent管理层（Agent Orchestration）
任务调度器（Agent Scheduler）
    当前活跃的Agent实例注册表，NPC-llm实例映射
    任务/请求队列，负载均衡（显卡能力检测、推理延迟、节点压力评估）
节点能力发现与卸载机制
    节点上下线自动通知主机（“agent节点自动注册–心跳”）
    零能力节点/弱机“静默模式”
    Agent迁移/同步（热插拔式支持）

